#!/usr/bin/env bash
# Script that displays the PID and name of processes containing the word "bash"

for pid in /proc/[0-9]*; do
  if grep -q bash "$pid/comm" 2>/dev/null; then
    cmd=$(cat "$pid/comm" 2>/dev/null)
    echo "$(basename "$pid") $cmd"
  fi
done
